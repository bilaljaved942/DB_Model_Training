# ─────────────────────────────────────────────────────────────────────────────
# Multi-Database SLM — Production Requirements
# Python 3.11+
# ─────────────────────────────────────────────────────────────────────────────

# ── Core Data & Ingestion ────────────────────────────────────────────────────
pandas>=2.1.0
openpyxl>=3.1.0          # Excel (.xlsx) reader for Online Retail
numpy>=1.26.0
sqlglot>=23.0.0          # SQL parsing and schema validation

# ── SLM Training & Inference (CUDA GPU / Google Colab) ───────────────────────
# torch>=2.2.0
# transformers>=4.43.0
# bitsandbytes>=0.43.0    # 4-bit quantization
# accelerate>=0.33.0
# peft>=0.12.0            # LoRA / QLoRA
# trl>=0.9.0              # SFTTrainer

# ── Inference APIs & Utilities ───────────────────────────────────────────────
requests>=2.31.0          # Ollama REST API calls
groq>=0.9.0               # Groq Cloud API fallback
python-dotenv>=1.0.0      # .env for API keys
rich>=13.7.0              # Terminal formatting
tabulate>=0.9.0           # Table formatting
tqdm>=4.66.0              # Progress bars
colorama>=0.4.6           # Terminal colors
