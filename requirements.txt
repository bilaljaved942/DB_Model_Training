# ─────────────────────────────────────────────────────────────────────────────
# Multi-Database SLM — Production Requirements
# Python 3.11+
# ─────────────────────────────────────────────────────────────────────────────

# ── Core Data ────────────────────────────────────────────────────────────────
pandas>=2.1.0
openpyxl>=3.1.0          # Excel (.xlsx) reader for Online Retail
numpy>=1.26.0

# ── SQL Safety & Parsing ─────────────────────────────────────────────────────
sqlglot>=23.0.0           # SQL parse/validate/transpile (no hallucinated syntax)

# ── SLM Inference — Primary (Ollama, local, no GPU required) ─────────────────
requests>=2.31.0          # Ollama REST API calls (http://localhost:11434)

# ── SLM Inference — Fallback (HuggingFace, requires CUDA GPU) ────────────────
# Uncomment these if you want the HuggingFace fallback:
# torch>=2.1.0
# transformers>=4.45.0
# bitsandbytes>=0.43.0    # 4-bit quantization
# accelerate>=0.26.0
# peft>=0.10.0            # LoRA / QLoRA
# trl>=0.9.0              # SFTTrainer for fine-tuning

# ── Groq Cloud API — Last-resort Fallback ────────────────────────────────────
groq>=0.9.0               # Free-tier fast inference (llama-3.1-70b)

# ── Streamlit Dashboard ───────────────────────────────────────────────────────
streamlit>=1.38.0
plotly>=5.20.0
streamlit-extras>=0.4.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0      # .env for API keys
rich>=13.7.0              # Beautiful CLI output for eval suite
tabulate>=0.9.0           # Table formatting
tqdm>=4.66.0              # Progress bars for DB setup
colorama>=0.4.6           # Cross-platform terminal colors
